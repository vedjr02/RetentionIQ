# Backend environment (copy to backend/.env)
DATABASE_URL=postgresql+psycopg://postgres:@/postgres?host=/private/tmp/retentioniq_pgdata
CORS_ORIGINS=http://localhost:3000

# Production example (Neon / Supabase / Railway Postgres):
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/retentioniq
# CORS_ORIGINS=https://your-app.vercel.app,http://localhost:3000
